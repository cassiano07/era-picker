import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#302f34',
    maxWidth: 300,
    width: 300,
    borderRadius: 10,
    padding: 10,
    gap: 10,
    position: 'absolute',
    zIndex: 2
  },
});

export {
  styles
};